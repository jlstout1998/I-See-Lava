[versions]
# Mod Metadata
modVersion = "1.2.1"
mavenGroup = "mod.azure.iseelava"
modName = "iseelava"

# Toolchain and Environment
java = "25"
minecraft = "26.1.1"
pack = "[84, 0]"            # Resource Pack Version

# Fabric Components
loom = "1.16.1"             # Fabric Loom Version
fabric = "0.145.4+26.1.1"   # Fabric API Version
fabricLoader = "0.18.6"     # Fabric Loader Version

# Sodium
sodium = "0.8.9+mc26.1.1"   # Sodium Version

[libraries]
# Dependencies in the build.gradle
minecraft = { group = "com.mojang", name = "minecraft", version.ref = "minecraft" }
fabric-loader = { group = "net.fabricmc", name = "fabric-loader", version.ref = "fabricLoader" }
fabric-api = { group = "net.fabricmc.fabric-api", name = "fabric-api", version.ref = "fabric" }
sodium = { group =  "net.caffeinemc", name = "sodium-fabric", version.ref = "sodium" }

[plugins]
# Plugin in the build.gradle
loomID = { id = "net.fabricmc.fabric-loom", version.ref = "loom" }
